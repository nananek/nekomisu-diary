# Production compose for Exchange Diary.
# All services share Tailscale's network namespace and communicate on 127.0.0.1.
# The tailscale_state volume persists the node identity across restarts.

name: diary-prod

services:
  tailscale:
    image: tailscale/tailscale:latest
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - NET_RAW
    environment:
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_EXTRA_ARGS=--advertise-exit-node=false
      - TS_SERVE_CONFIG=/config/serve.json
    volumes:
      - ./tailscale_state:/var/lib/tailscale
      - ./tailscale-serve.json:/config/serve.json:ro
      - /dev/net/tun:/dev/net/tun

  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    depends_on:
      - tailscale
    environment:
      - POSTGRES_DB=diary
      - POSTGRES_USER=diary
      - POSTGRES_PASSWORD=${PG_PASSWORD}
      - POSTGRES_INITDB_ARGS=--locale=C.UTF-8 --encoding=UTF8
    volumes:
      - pg-data:/var/lib/postgresql/data
      - ./schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      - ./tools/seed-data.sql:/docker-entrypoint-initdb.d/02-seed-data.sql:ro
    network_mode: "service:tailscale"

  server:
    image: alpine:latest
    restart: unless-stopped
    depends_on:
      - postgres
      - tailscale
    entrypoint: ["/app/bin/server"]
    command:
      - -pg=postgres://diary:${PG_PASSWORD}@127.0.0.1:5432/diary?sslmode=disable
      - -addr=:3000
      - -uploads=/app/uploads
      - -web=/app/web/dist
      - -rp-id=${RP_ID}
      - -rp-origin=${RP_ORIGIN}
      - -discord-webhook=${DISCORD_WEBHOOK:-}
    volumes:
      - ./bin/server:/app/bin/server:ro
      - ./bin/passwdreset:/app/bin/passwdreset:ro
      - ./web/dist:/app/web/dist:ro
      - ./uploads:/app/uploads
    network_mode: "service:tailscale"

volumes:
  pg-data:
